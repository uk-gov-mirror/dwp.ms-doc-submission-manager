{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "decision-data-v1.json",
  "type": "object",
  "title": "decision-data-v1",
  "description": "Decision data used to notify coordinator that a decision has been made",
  "required": [
    "application_id",
    "decision_id",
    "outcome",
    "daily_living",
    "mobility"
  ],
  "properties": {
    "application_id": {
      "type": "string",
      "description": "The application id for which decision has been made"
    },
    "decision_id": {
      "type": "string",
      "description": "The id for decision"
    },
    "decision_type": {
      "$ref": "decision-type.json"
    },
    "nino": {
      "type": "string",
      "description": "The nino for the application"
    },
    "outcome": {
      "$ref": "decision-outcome.json"
    },
    "daily_living": {
      "$ref": "award-rate.json"
    },
    "mobility": {
      "$ref": "award-rate.json"
    },
    "review_period": {
      "$ref": "review-period.json"
    },
    "light_touch_review": {
      "type": "boolean",
      "description": "Set to true if review period is to be light touch"
    },
    "review_date": {
      "type": "string",
      "format": "date",
      "description": "Award review date if decision outcome is allowed"
    },
    "award_start_date": {
      "type": "string",
      "format": "date",
      "description": "Award start date if decision outcome is allowed"
    },
    "award_end_date": {
      "type": "string",
      "format": "date",
      "description": "Award end date if decision outcome is allowed. In case award is ongoing, this date is set to 2099-12-12"
    },
    "payable_from_date": {
      "type": "string",
      "format": "date",
      "description": "Payable from date if decision outcome is allowed"
    },
    "decision_date": {
      "type": "string",
      "format": "date",
      "description": "Date when a decision has been made"
    },
    "frequency": {
      "$ref": "payment-frequency.json"
    }
  }
}
